# Unsupervised_Sentiment_Analysis
GPT-4, a potential successor to GPT-3, undergoes unsupervised sentiment analysis using a Kaggle tweet dataset. Each tweet is labeled with sentiment scores (-1 to 1). Our K-means model's results compete favorably with BERT and VADER benchmarks.
